---
slug: components-avatar
title: Avatar
description: An image element representing the user, with a fallback
links:
    api: https://www.radix-ui.com/docs/primitives/components/avatar#api-reference
---

<DemoCodePreview
    path="demo/docs/avatar/default.tsx"
    props={[
        {
            title: "Shape",
            name: "shape",
            type: "select",
            defaultValue: "round",
            values: ["round", "square"],
        },
        {
            title: "Size",
            name: "size",
            type: "select",
            defaultValue: "md",
            values: ["sm", "md", "lg"],
        },
    ]}
/>

## Installation

<Steps>
    <Step>Install the following dependencies</Step>
    <InstallationCommand command="npm install @radix-ui/react-avatar" />
    <Step>Copy and paste the following code into your project</Step>
    <CodePreview path="components/ui/avatar.tsx" collapsible></CodePreview>
    <Step>Update the import paths to fit your project setup</Step>
</Steps>

## Examples

### Only Text

If only `AvatarFallback` is provided, it will display the fallback content

<DemoCodePreview
    path="demo/docs/avatar/only-text.tsx"
    props={[
        {
            title: "Shape",
            name: "shape",
            type: "select",
            defaultValue: "round",
            values: ["round", "square"],
        },
        {
            title: "Size",
            name: "size",
            type: "select",
            defaultValue: "md",
            values: ["sm", "md", "lg"],
        },
        {
            title: "Color",
            name: "color",
            type: "select",
            defaultValue: "default",
            values: ["default", "primary", "secondary", "error"],
        },
    ]}
/>

### Icon

Use `Icon` in `AvatarFallback` to display an icon as avatar

<DemoCodePreview
    path="demo/docs/avatar/icon.tsx"
    props={[
        {
            title: "Shape",
            name: "shape",
            type: "select",
            defaultValue: "round",
            values: ["round", "square"],
        },
        {
            title: "Size",
            name: "size",
            type: "select",
            defaultValue: "md",
            values: ["sm", "md", "lg"],
        },
        {
            title: "Color",
            name: "color",
            type: "select",
            defaultValue: "default",
            values: ["default", "primary", "secondary", "error"],
        },
    ]}
/>

## Props

| Prop  | Type                                    | Default |
| ----- | --------------------------------------- | ------- |
| shape | `round` `square`                        | round   |
| size  | `sm` `md` `lg`                          | md      |
| color | `default` `primary` `secondary` `error` | default |
