---
slug: components-tabs
title: Tabs
description: Allows users to switch between different sections of content within the same area
links:
    api: https://www.radix-ui.com/primitives/docs/components/tabs#api-reference
---

<DemoCodePreview
    path="demo/docs/tabs/default.tsx"
    props={[
        {
            title: "Variant",
            name: "variant",
            type: "select",
            defaultValue: "underline",
            values: ["underline", "ghost"],
        },
    ]}
/>

## Installation

<Steps>
    <Step>
        Install the following dependencies
    </Step>
    <InstallationCommand command="npm install @radix-ui/react-tabs" />
    <Step>
        Copy and paste the following code into your project
    </Step>
    <CodePreview path='components/ui/tabs.tsx' collapsible></CodePreview>
    <Step>
        Update the import paths to fit your project setup
    </Step>
</Steps>

## Examples

### Disabled

<DemoCodePreview
    path="demo/docs/tabs/disabled.tsx"
    props={[
        {
            title: "Variant",
            name: "variant",
            type: "select",
            defaultValue: "underline",
            values: ["underline", "ghost"],
        },
    ]}
/>

### Icon

<DemoCodePreview
    path="demo/docs/tabs/icon.tsx"
    props={[
        {
            title: "Variant",
            name: "variant",
            type: "select",
            defaultValue: "underline",
            values: ["underline", "ghost"],
        },
    ]}
/>

### Controlled

<DemoCodePreview
    path="demo/docs/tabs/controlled.tsx"
    props={[
        {
            title: "Variant",
            name: "variant",
            type: "select",
            defaultValue: "underline",
            values: ["underline", "ghost"],
        },
    ]}
/>

## Props

| Prop    | Type                 | Default   |
|---------|----------------------|-----------|
| variant | `underline` `ghost ` | underline |
